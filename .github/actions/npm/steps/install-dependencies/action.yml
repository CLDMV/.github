name: "Install Dependencies"
description: "Install dependencies using the appropriate package manager"

inputs:
  package-manager:
    description: "Package manager to use: auto (default, detect), or explicit npm | yarn | pnpm"
    required: false
    default: "auto"

runs:
  using: "node24"
  main: "action.mjs"
