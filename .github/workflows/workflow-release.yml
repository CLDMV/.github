#
# @Project: workflows
# @Filename: /workflow-release.yml
# @Date: 2026-02-21 12:59:41 -08:00 (1771707581)
# @Author: Nate Corcoran <CLDMV>
# @Email: <Shinrai@users.noreply.github.com>
# -----
# @Last modified by: Nate Corcoran <CLDMV> (Shinrai@users.noreply.github.com)
# @Last modified time: 2026-04-11 16:52:01 -07:00 (1775951521)
# -----
# @Copyright: Copyright (c) 2013-2026 Catalyzed Motivation Inc. All rights reserved.
#

# CLDMV/.github/.github/workflows/release.yml
name: 🚀 Release Workflow (Org-Level)

on:
    workflow_call:
        inputs:
            package_name:
                required: true
                type: string
                description: "NPM package name (e.g., @cldmv/slothlet)"
            debug:
                required: false
                type: boolean
                default: false
                description: "Enable debug logging for troubleshooting"
            dry_run:
                required: false
                type: boolean
                default: false
                description: "Dry run mode - validate everything but don't create PR or make changes"
            node_version:
                required: false
                type: string
                default: "lts/*"
                description: "Node.js version to use (default: lts/*)"
            min_node_version:
                required: false
                type: string
                default: "20"
                description: "Minimum Node.js version for matrix testing (default: 20, oldest non-EOL)"
            max_node_major:
                required: false
                type: string
                default: "22"
                description: "Override max Node.js major version (default: 22)"
            lts_only_matrix:
                required: false
                type: boolean
                default: true
                description: "Only include even-numbered (LTS) Node.js majors in the test matrix. Required when deps declare engines that exclude odd-numbered releases (e.g. rolldown)."
            package_manager:
                required: false
                type: string
                default: "npm"
                description: "Package manager (npm or yarn)"
            test_command:
                required: false
                type: string
                default: "npm test"
                description: "Command to run tests"
            build_command:
                required: false
                type: string
                default: "npm run build:ci"
                description: "Command to build package"
            version_bump:
                required: false
                type: string
                default: ""
                description: "Type of version bump (major, minor, patch). Leave empty for auto-detection from commit message."
            # Optional release parameters
            version:
                required: false
                type: string
                default: ""
                description: "Specific version for release (auto-calculated if not provided)"
            is_prerelease:
                required: false
                type: boolean
                default: false
                description: "Whether this is a prerelease"
            release_source_only:
                required: false
                type: boolean
                default: false
                description: "Whether to create source-only release (no package assets)"
            create_documentation:
                required: false
                type: boolean
                default: true
                description: "Whether to create/update VERSION_TAGS.md"
            # Test control options
            skip_performance_tests:
                required: false
                type: boolean
                default: false
                description: "Skip performance tests during CI"
            skip_matrix_tests:
                required: false
                type: boolean
                default: false
                description: "Skip matrix testing (use single node version only)"
            test_environment:
                required: false
                type: string
                default: "development"
                description: "Environment for tests (affects NODE_ENV and NODE_OPTIONS --conditions flag)"
            # Bot signing parameters
            use_gpg:
                required: false
                type: boolean
                default: false
                description: "If true, forward provided secrets to enable GPG signing"
        outputs:
            release-pr-created:
                description: "Whether a release PR was created"
                value: ${{ jobs.release.outputs.pr-created }}
            release-pr-number:
                description: "The release PR number if created"
                value: ${{ jobs.release.outputs.pr-number }}
            new-version:
                description: "The new version that was set"
                value: ${{ jobs.release.outputs.new-version }}

        secrets:
            NPM_TOKEN:
                description: "NPM token for publishing packages"
                required: false
            BOT_APP_CLIENT_ID:
                description: "GitHub App Client ID for CLDMV bot"
                required: false
            BOT_APP_ID:
                description: "[DEPRECATED] Use BOT_APP_CLIENT_ID instead"
                required: false
            BOT_APP_PRIVATE_KEY:
                description: "GitHub App private key for CLDMV bot"
                required: false
            TAGGER_NAME:
                description: "GPG tagger name for signing"
                required: false
            TAGGER_EMAIL:
                description: "GPG tagger email for signing"
                required: false
            GPG_PRIVATE_KEY:
                description: "ASCII-armored private key for signing"
                required: false
            GPG_PASSPHRASE:
                description: "Passphrase for the private key (if protected)"
                required: false

jobs:
    check-existing-pr:
        name: "🔍 Check for Existing PR"
        runs-on: ubuntu-latest
        outputs:
            pr-exists: ${{ steps.check.outputs.pr-exists }}
            pr-number: ${{ steps.check.outputs.pr-number }}
            should-skip: ${{ steps.check.outputs.should-skip }}
        steps:
            - name: Check for existing release PR
              id: check
              uses: CLDMV/.github/.github/actions/github/steps/check-existing-pr@v2
              with:
                  github-token: ${{ github.token }}
                  head-branch: ${{ github.ref_name }}
                  base-branch: ${{ github.event.repository.default_branch }}

    detect-release:
        name: "🔍 Detect Release"
        if: needs.check-existing-pr.outputs.should-skip == 'false'
        needs: check-existing-pr
        runs-on: ubuntu-latest
        outputs:
            should-release: ${{ steps.check.outputs.should-create-pr }}
            version-bump: ${{ steps.check.outputs.version-bump }}
            has-breaking: ${{ steps.check.outputs.has-breaking }}
        steps:
            - name: Checkout code
              uses: CLDMV/.github/.github/actions/common/steps/checkout-code@v2
              with:
                  fetch-depth: 0

            - name: Check Release Commit
              id: check
              uses: CLDMV/.github/.github/actions/git/steps/check-release-commit@v2
              env:
                  DEBUG: ${{ inputs.debug || 'true' }}

    update-existing-pr:
        name: "📝 Update Existing PR"
        if: needs.check-existing-pr.outputs.should-skip == 'true'
        needs: check-existing-pr
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: CLDMV/.github/.github/actions/common/steps/checkout-code@v2
              with:
                  fetch-depth: 0

            - name: Setup Node.js
              uses: CLDMV/.github/.github/actions/common/steps/setup-node@v2

            - name: Get branch divergence point
              id: divergence
              uses: CLDMV/.github/.github/actions/git/steps/find-divergence@v2

            - name: Check release commit type
              id: release-check
              uses: CLDMV/.github/.github/actions/git/steps/check-release-commit@v2
              with:
                  base-ref: ${{ steps.divergence.outputs.merge-base }}
                  base-version: ${{ steps.divergence.outputs.base-version }}
              env:
                  DEBUG: ${{ inputs.debug || 'true' }}

            - name: Create App token
              id: app-token
              uses: CLDMV/.github/.github/actions/github/steps/create-app-token@v2
              with:
                  client_id: ${{ secrets.BOT_APP_CLIENT_ID || secrets.BOT_APP_ID }}
                  private_key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
              env:
                  BOT_APP_CLIENT_ID: ${{ secrets.BOT_APP_CLIENT_ID || secrets.BOT_APP_ID }}
                  BOT_APP_PRIVATE_KEY: ${{ secrets.BOT_APP_PRIVATE_KEY }}

            - name: Calculate new version
              id: version
              if: steps.release-check.outputs.should-create-pr == 'true'
              uses: CLDMV/.github/.github/actions/npm/steps/calculate-version@v2
              with:
                  version-bump: ${{ steps.release-check.outputs.version-bump }}
                  explicit-version: ${{ steps.release-check.outputs.explicit-version }}
                  base-version: ${{ steps.divergence.outputs.base-version }}

            - name: Install dependencies
              if: steps.release-check.outputs.should-create-pr == 'true'
              uses: CLDMV/.github/.github/actions/npm/steps/install-dependencies@v2
              with:
                  package-manager: ${{ inputs.package_manager }}

            - name: Build package
              if: steps.release-check.outputs.should-create-pr == 'true'
              uses: CLDMV/.github/.github/actions/common/steps/build-project@v2
              with:
                  build-command: ${{ inputs.build_command }}
                  environment: production

            - name: Update package version
              if: steps.release-check.outputs.should-create-pr == 'true'
              uses: CLDMV/.github/.github/actions/npm/steps/update-package-version@v2
              with:
                  new-version: ${{ steps.version.outputs.new-version }}
                  package-manager: ${{ inputs.package_manager }}

            - name: Restore src/ deleted by build
              if: steps.release-check.outputs.should-create-pr == 'true'
              uses: CLDMV/.github/.github/actions/git/steps/restore-deleted-src@v2

            - name: Create signed commit
              if: steps.release-check.outputs.should-create-pr == 'true'
              uses: CLDMV/.github/.github/actions/github/api/commit@v2
              with:
                  commit-message: "chore: bump version to ${{ steps.version.outputs.new-version }}"
                  github-token: ${{ steps.app-token.outputs.token }}

            - name: Determine display version
              id: display-version
              uses: CLDMV/.github/.github/actions/npm/steps/resolve-version@v2
              with:
                  new-version: ${{ steps.version.outputs.new-version }}

            - name: Get PR commit range
              id: get-pr-range
              uses: CLDMV/.github/.github/actions/github/steps/get-pr-commit-range@v2
              with:
                  github-token: ${{ steps.app-token.outputs.token }}
                  pr-number: ${{ needs.check-existing-pr.outputs.pr-number }}

            - name: Generate updated changelog
              id: changelog
              uses: CLDMV/.github/.github/actions/git/steps/generate-comprehensive-changelog@v2
              with:
                  commit-range: ${{ steps.get-pr-range.outputs.commit-range }}

            - name: Update PR title and description
              uses: CLDMV/.github/.github/actions/github/steps/update-pr-changelog@v2
              with:
                  github-token: ${{ steps.app-token.outputs.token }}
                  pr-number: ${{ needs.check-existing-pr.outputs.pr-number }}
                  new-version: ${{ steps.display-version.outputs.version }}
                  changelog-content: ${{ steps.changelog.outputs.changelog-content }}

            - name: Compute raw label aliases
              id: raw-labels
              if: steps.release-check.outputs.should-create-pr == 'true'
              uses: CLDMV/.github/.github/actions/github/steps/compute-label-aliases@v2
              with:
                  bump-type: ${{ steps.release-check.outputs.version-bump }}

            - name: Resolve PR label aliases
              id: resolved-labels
              if: steps.release-check.outputs.should-create-pr == 'true'
              uses: CLDMV/.github/.github/actions/github/steps/resolve-labels@v2
              with:
                  aliases: ${{ steps.raw-labels.outputs.labels }}

            - name: Sync PR labels
              if: steps.release-check.outputs.should-create-pr == 'true'
              uses: CLDMV/.github/.github/actions/github/steps/sync-pr-labels@v2
              with:
                  github-token: ${{ steps.app-token.outputs.token }}
                  pr-number: ${{ needs.check-existing-pr.outputs.pr-number }}
                  labels: ${{ steps.resolved-labels.outputs.labels }}

    build-and-test:
        name: "🏗️ Build and Test"
        if: needs.check-existing-pr.outputs.should-skip == 'false' && needs.detect-release.outputs.should-release == 'true'
        needs: [check-existing-pr, detect-release]
        uses: CLDMV/.github/.github/workflows/reusable-build-and-test.yml@v2
        with:
            package_name: ${{ inputs.package_name }}
            debug: ${{ inputs.debug }}
            run_build_and_test: true
            node_version: ${{ inputs.node_version }}
            min_node_version: ${{ inputs.min_node_version }}
            max_node_major: ${{ inputs.max_node_major }}
            lts_only_matrix: ${{ inputs.lts_only_matrix }}
            package_manager: ${{ inputs.package_manager }}
            test_command: ${{ inputs.test_command }}
            build_command: ${{ inputs.build_command }}
            skip_performance_tests: ${{ inputs.skip_performance_tests }}
            skip_matrix_tests: ${{ inputs.skip_matrix_tests }}
            test_environment: ${{ inputs.test_environment }}

    release:
        name: "🚀 Release Management"
        if: needs.check-existing-pr.outputs.should-skip == 'false' && needs.detect-release.outputs.should-release == 'true'
        needs: [check-existing-pr, detect-release, build-and-test]
        uses: CLDMV/.github/.github/workflows/reusable-release-management.yml@v2
        with:
            package_name: ${{ inputs.package_name }}
            debug: ${{ inputs.debug }}
            dry_run: ${{ inputs.dry_run }}
            # Job control flags - only run create release PR since we already detected
            run_detect_release: false
            run_create_release_pr: true
            # Build parameters
            node_version: ${{ inputs.node_version }}
            package_manager: ${{ inputs.package_manager }}
            version_bump: ${{ needs.detect-release.outputs.version-bump }}
            version: ${{ inputs.version }}
        secrets:
            BOT_APP_CLIENT_ID: ${{ secrets.BOT_APP_CLIENT_ID }}
            BOT_APP_PRIVATE_KEY: ${{ secrets.BOT_APP_PRIVATE_KEY }}

    summary:
        name: "📋 Release Summary"
        needs:
            [
                check-existing-pr,
                detect-release,
                update-existing-pr,
                build-and-test,
                release,
            ]
        if: always()
        runs-on: ubuntu-latest
        steps:
            - name: Generate Release Summary
              uses: CLDMV/.github/.github/actions/workflows/release-summary@v2
              with:
                  dry-run: ${{ inputs.dry_run }}
                  package-name: ${{ inputs.package_name }}
                  update-existing-pr-result: ${{ needs.update-existing-pr.result }}
                  existing-pr-number: ${{ needs.check-existing-pr.outputs.pr-number }}
                  should-release: ${{ needs.detect-release.outputs.should-release }}
                  release-result: ${{ needs.release.result }}
                  release-pr-created: ${{ needs.release.outputs.pr-created }}
                  release-new-version: ${{ needs.release.outputs.new-version }}
                  release-pr-number: ${{ needs.release.outputs.pr-number }}
                  server-url: ${{ github.server_url }}
                  repository: ${{ github.repository }}
